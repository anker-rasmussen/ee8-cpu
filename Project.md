I am creating an assignment for second year Engineering Systems electrical engineering students at university (undergrad level). I want to assign coursework where they are building a very simple 8 bit cpu. Background knowledge of the students includes building a 4bit adder subtractor (and output to a 7segment). I want to flesh out the assignment a bit more - I would like 3bit opcode so they can have 8 different types of instruction (ADD, MUL etc..) I would also like for the ability to have 2-4 registers so I can create commands such as "ADD register0 register1 register2" where it would add registers 0 and 1, and output to 3. They have created the adder in Logisim which has capability to create modules and copypaste them. I need to make some lesson plans to decompose the concepts of a CPU. This week we will be doing memory with PIPO and d latches (effectively the cache for the cpu). From here on out we need to introduce an ALU (atomic operations - not sure about adding divide). then move onto things like program counter etc.. 
